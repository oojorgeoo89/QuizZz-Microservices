spring:
  application:
    name: api-gateway

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
      
zuul:
  routes:
    quizzz:
      path: /**
      serviceId: quizzz
      sensitive-headers: 